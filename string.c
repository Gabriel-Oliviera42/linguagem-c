// strcpy(X,Y) Realiza a cópia do conteúdo de uma variável a outra.

// strncpy(X,Y,n) Realiza a cópia do conteúdo de uma variável a outra, porém, deve ser especificado o tamanho a ser copiado.

// strcat(X, "alguma coisa") Realiza a concatenação do conteúdo de uma variável a outra.

// strncat(X, Y, n) Realiza a concatenação do conteúdo de uma variável a outra, porém, deve ser especificado o tamanho a ser concatenado.

// int = strlen(X) variável do tipo inteiro = strlen(string);

/*

variável do tipo inteiro = strcmp(string1, string2);

Compara o conteúdo de duas strings;

Possíveis valores de retorno:

0: conteúdo das strings são iguais

< 0: conteúdo da string1 é menor do que string2

> 0: conteúdo da string1 é maior do que string2

*/

/*

variável do tipo inteiro = strncmp(string1, string2, tamanho);

Também faz a comparação do conteúdo de duas strings, porém, deve ser especificado o tamanho a ser comparado;

Possíveis valores de retorno:

0: conteúdo das strings são iguais

< 0: conteúdo da string1 é menor do que string2

> 0: conteúdo da string1 é maior do que string2

*/